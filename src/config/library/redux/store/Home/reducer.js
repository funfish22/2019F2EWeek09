import { Types } from './action'

const initState = {
    searchStar: false
}


const Home = (state = initState, action) => {
    switch(action.type){

        default:
            return state;
    }
}

export default Home;